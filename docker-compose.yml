version: '3.7'

services:
  client:
    build:
      context: ./
      dockerfile: ./Dockerfile-client
    container_name: client
    volumes:
      - ./dist/frontend:/app/build # TODO amit konténeren belül elkészített, rakj a dist mappába
      - /app/node_modules

  server:
    build:
      context: ./
      target: production-build-stage
      dockerfile: Dockerfile
    container_name: server
    ports:
      - '3000:3000'
    environment:
      MONGO_HOST: mongo
      MONGO_PORT: 27017
      MONGO_DATABASE: mongoose
    volumes:
      - ./:/app
      - /app/node_modules
      - ./uploads:/var/www/public
    restart: 'unless-stopped'
    networks:
      - backend
    links:
      - mongo
    depends_on:
      - mongo
      - client


  mongo:
    image: mvertes/alpine-mongo
    container_name: mongo
    networks:
      - backend

networks:
  backend:
    driver: bridge

volumes:
  data:
    driver: local
